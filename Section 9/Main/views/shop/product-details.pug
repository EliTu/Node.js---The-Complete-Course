extends ../layouts/main-layout.pug

block content 
    h2 #{pageSubtitle}
    main
        if productDetails.id
            ul
                each detail in productDetails
                    if detail !== '' && detail !== productDetails.id
                        li #{detail}
